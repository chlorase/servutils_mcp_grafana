TODOs:

- server side access first basic setups:
    - graf, prometh
    >done
    - relocate the prometheus.yml under example_metrics-logs_emitter/ to example_metrics-logs_emitter/provisioning/prometheus
    >done

    - fix all .yml filees cannot reference .env vars.
    >done
    - bleh delete  examples copy of the prometheus.yml
    >done

    - refactoring and get all prometheus, grafana working.
    >done
    - setup grafana gen fake data for now
    >done
    - basic mcp graf server setup and test.

- Hookup test LLM client


- client/agent proxy
    - and testable
- host app, using client.
- setup basic slackbot hookup
- save datapoints to redis allow? maybe even an api for that to transfer?

- add multi tenant